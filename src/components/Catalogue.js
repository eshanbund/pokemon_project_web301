import React from 'react';
import { NavLink, Switch, Route } from 'react-router-dom';

const Catalogue=(props)=>{
    return (
        <div>
            <p>Catalogue  {props.activepokemontest}</p>

        </div>
    );
}

export default Catalogue;